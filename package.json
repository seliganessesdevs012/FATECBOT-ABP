{
  "name": "fatecbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "build": "pnpm -r build"
  },
  "overrides": {
    "axios": "1.6.8"
  },
  "devDependencies": {
    "@types/node": "^25.5.2"
  },
  "dependencies": {
    "zustand": "^5.0.12"
  }
}